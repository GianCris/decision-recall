name: Decision Recall Core

on:
  push:
    branches:
      - agent/decision-recall-core-v0.1
  pull_request:
    paths:
      - 'decision_recall/**'
      - 'dr_bench/**'
      - 'dr_baselines/**'
      - 'tests/**'
      - 'pyproject.toml'
      - '.github/workflows/decision-recall-core.yml'

jobs:
  core-tests:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Run Decision Recall core tests
        run: python -m unittest discover -s tests -p 'test_decision_recall_milestone1.py' -v

  repository-offline-regression:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout full git history
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install repository package
        run: python -m pip install -e .

      - name: Run deterministic/offline repository regression excluding dedicated core suite
        shell: bash
        run: |
          python - <<'PY'
          from pathlib import Path
          import subprocess
          import sys

          # Historical-analysis suites below depend on archived local experiment
          # outputs that are intentionally not committed. The dedicated core suite
          # runs in the separate core-tests job, so exclude it here to keep counts
          # disjoint and claims unambiguous.
          excluded = {
              'test_decision_recall_milestone1.py',
              'test_dev_autopsy.py',
              'test_pauto_provider_probe.py',
              'test_round_b_recovered_analysis.py',
          }

          files = [
              str(path)
              for path in sorted(Path('tests').glob('test_*.py'))
              if path.name not in excluded
          ]
          print('Offline non-core regression files:', len(files))
          result = subprocess.run(
              [sys.executable, '-m', 'unittest', '-v', *files],
              check=False,
          )
          raise SystemExit(result.returncode)
          PY
